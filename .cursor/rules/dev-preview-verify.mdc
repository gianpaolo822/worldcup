---
description: 前端改动完成后必须重启 dev 并验证预览为最新代码
alwaysApply: true
---

# 本地预览验证（worldcup-web）

涉及 `worldcup-web/` 或 `data/` 的前端可见改动，**完成任务前必须**执行以下流程，并在回复中给出可验证的最新预览。

## 1. 重启开发服

```bash
cd worldcup-web && npm run dev:clean
```

- 使用 `dev:clean`，不要只跑 `npm run dev`（5173 旧进程会导致 HMR 失效、预览仍是旧代码）。
- 确认终端出现 `Local: http://127.0.0.1:5173/`。

## 2. 验证磁盘代码 = 开发服代码

改动文件中的**关键字符串**（类名、函数名等）必须同时出现在：

- 磁盘：`worldcup-web/src/...`
- 开发服：`curl -s http://127.0.0.1:5173/src/pages/<File>.tsx | rg '<关键字符串>'`

两者不一致 → 视为**未交付**，继续排查缓存/旧进程，不得声称已修复。

## 3. 回复中必须提供

1. **预览 URL**（带 cache buster）：`http://127.0.0.1:5173/<path>?v=<unix秒>`
2. **验证说明**：列出 1～2 个仅在最新代码中存在的特征（如类名、文案）
3. **操作步骤**：用户应如何操作页面确认（如「进球 Tab → 红牌 Tab，Tab 栏不应跳动」）

## 4. 禁止

- 在未执行第 1～2 步前说「请刷新看看」
- 只改磁盘代码而不重启/验证开发服
- 把 Vercel 线上地址当作本地改动已生效的依据
